(function () {
  'use strict';

  describe('Service: LanguageManagerService', function() {

    var LanguageManagerService, $rootScope;

    beforeEach(module('angularstudy.service.languagemanager'));

    beforeEach(inject(function (_$rootScope_, _LanguageManagerService_) {
      LanguageManagerService = _LanguageManagerService_;
      $rootScope = _$rootScope_;
    }));

    describe('someThing', function() {
      it('someThing', function() {

      });
    });
  });
})();
